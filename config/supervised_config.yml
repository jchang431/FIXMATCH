data:
  dataset: CIFAR
  label_pct: 0.1 # 0.1, 0.25, 0.5, 1.0:label portion

train:
  batch_size: 128
  lr: 0.0003
  n_epochs: 16
  num_workers: 4

optimizer:
  type: adamw
  weight_decay: 0.001

network:
  hidden_dim: 400
  proj_dim: 128

simclr:
  temperature: 0.5
