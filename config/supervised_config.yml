seed: 42

data:
  dataset: CIFAR
  label_pct: 0.1
  val_ratio: 0.1    
  num_classes: 10
  use_randaugment: true #if it is true, supervised(RA)

train:
  batch_size: 64
  lr: 0.001
  n_epochs: 100
  num_workers: 4

optimizer:
  type: adam
  weight_decay: 0.0001
  momentum: 0.9
  nesterov: true
