seed: 42

data:
  label_pct: 0.1   # 0.1 / 0.25 / 0.5 / 1.0
  val_ratio: 0.1

dataset:
  num_classes: 10

train:
  batch_size: 64
  mu: 7
  epochs: 50
  lr: 0.03
  num_workers: 4

fixmatch:
  threshold: 0.95
  lambda_u: 1.0
